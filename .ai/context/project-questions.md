# Project Questions (Open)

1. Нужна ли поддержка long-lived daemon режима для MCP-сервера между workspace?
2. Нужна ли отдельная стратегия OAuth UX для headless-сред (CI/remote)?
3. Нужна ли единая политика rate-limit backoff для всех tool-ов с конфигурируемыми лимитами?
4. Какие минимальные метрики/логи обязательны для production troubleshooting?
